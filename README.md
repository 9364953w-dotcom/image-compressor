# 图片批量压缩工具 v1.1.0

一个基于 PyQt5 的图形化图片批量压缩工具，支持多线程并行处理、智能压缩、格式转换等高级功能。

## ✨ 功能特性

### 基础功能
- 📁 **文件夹拖拽选择** - 支持拖拽文件夹到输入框
- 🖼️ **多格式支持** - JPG、PNG、WebP、BMP、TIFF
- 🔄 **覆盖模式** - 可选择输出文件夹或直接覆盖原文件
- 📂 **子文件夹递归** - 支持包含子文件夹处理
- ⚡ **多线程并行** - 智能线程数控制

### 新增高级功能（v1.1.0）

#### 1. 尺寸调整
- 限制最大宽度/高度
- 保持原始比例或强制调整
- 适合批量生成缩略图或统一尺寸

#### 2. 格式转换
- 保持原格式
- 统一转换为 JPG、PNG 或 WebP
- 自动处理颜色模式转换

#### 3. 智能压缩
- 自动寻找最佳质量参数
- 指定目标文件大小（如 200KB）
- 二分查找算法快速收敛

#### 4. 历史记录
- 自动保存输入/输出路径
- 下拉框快速选择历史记录
- 保存压缩设置偏好

#### 5. 批量重命名
- 保持原文件名
- 原文件名_序号（如 image_001.jpg）
- 纯序号（如 001.jpg）
- 自定义前缀_序号
- 日期_序号（如 20240301_001.jpg）

#### 6. 详细统计
- 实时表格展示每个文件的处理结果
- 显示原始大小、压缩后大小、节省比例
- 显示尺寸变化信息

#### 7. 智能线程控制
- 根据文件大小动态调整线程数
- 大文件（>10MB）：减少线程，避免内存溢出
- 小文件（<100KB）：增加线程，提高吞吐量

#### 8. 增量压缩
- 跳过已用相同设置处理过的文件
- 基于文件哈希和修改时间判断
- 缓存存储在用户目录，跨会话有效

## 📁 项目结构

```
image-compressor/
├── src/
│   ├── __init__.py
│   ├── __main__.py          # 程序入口
│   ├── config.py            # 配置常量 + ConfigManager
│   ├── utils.py             # 工具函数
│   ├── core/
│   │   ├── __init__.py
│   │   ├── compressor.py    # 图片压缩核心（含尺寸调整、格式转换、智能压缩）
│   │   └── worker.py        # 多线程工作器（含智能线程控制、详细统计）
│   ├── widgets/
│   │   ├── __init__.py
│   │   ├── drag_drop.py     # 拖拽输入框
│   │   └── main_window.py   # 主窗口（含新功能界面）
│   └── resources/
│       └── icon.icns        # 应用图标
├── 图片压缩工具.spec        # PyInstaller 打包配置
├── README.md
└── requirements.txt
```

## 🚀 安装使用

### 安装依赖

```bash
pip install PyQt5 Pillow
```

### 开发运行

```bash
cd image-compressor
python -m src
```

### 打包为可执行文件

```bash
pip install pyinstaller
pyinstaller 图片压缩工具.spec
```

## 📖 使用说明

### 基本使用

1. **选择输入文件夹**：点击"浏览..."按钮或拖拽文件夹到输入框
2. **选择输出文件夹**（可选）：
   - 选择输出文件夹：压缩后的图片保存到该文件夹
   - 留空：直接覆盖原文件（会弹出确认提示）
3. **设置选项**：根据需要调整各项参数
4. **开始压缩**：点击"开始压缩"按钮

### 高级功能使用

#### 尺寸调整
1. 勾选"调整尺寸"
2. 设置最大宽度/高度（像素）
3. 勾选"保持比例"以避免图片变形

#### 格式转换
1. 在"输出格式"下拉框选择目标格式
2. 可选择：保持原格式、转换为 JPG/PNG/WebP

#### 智能压缩
1. 勾选"智能压缩"
2. 设置目标大小（如 200KB）
3. 程序会自动尝试不同质量参数，找到最接近目标大小的设置

#### 批量重命名
1. 勾选"重命名文件"
2. 选择命名模式
3. 对于"自定义前缀"模式，输入自定义前缀文本

#### 增量压缩
1. 勾选"跳过已处理文件"
2. 程序会自动跳过用相同设置处理过的文件
3. 缓存文件存储在用户目录 `~/.image-compressor/`

#### 导出统计
1. 压缩完成后，点击"导出统计"按钮
2. 选择保存位置（CSV 格式）
3. 可用 Excel 打开查看详细统计

## 💡 使用技巧

### 场景1：网站图片优化
- 调整尺寸：最大宽度 1920px
- 输出格式：WebP
- 质量：80%
- 效果：大幅减小文件大小，提升网页加载速度

### 场景2：社交分享图片
- 调整尺寸：最大宽度 1080px
- 智能压缩：目标 200KB
- 效果：适合微信/微博分享，文件小且清晰度足够

### 场景3：批量归档整理
- 重命名：日期_序号
- 输出格式：JPG（兼容性最好）
- 质量：95%（高质量存档）

## 📊 压缩效果

| 原格式 | 原大小 | 设置 | 压缩后 | 节省 |
|--------|--------|------|--------|------|
| JPG | 5.2MB | 质量80% + 宽度1920 | 380KB | 92.7% |
| PNG | 2.8MB | 转WebP 质量85% | 420KB | 85.0% |
| BMP | 8.5MB | 转JPG 质量90% | 1.2MB | 85.9% |

## 🔧 技术栈

- Python 3.8+
- PyQt5 - GUI 框架
- Pillow - 图像处理
- PyInstaller - 打包工具

## 📜 许可证

MIT License

## 📝 更新日志

### v1.1.0 (2024-03-01)
- 新增：尺寸调整功能
- 新增：格式转换功能
- 新增：智能压缩功能
- 新增：历史记录功能
- 新增：批量重命名功能
- 新增：详细统计表格
- 新增：智能线程控制
- 新增：增量压缩功能

### v1.0.0 (2024-02-28)
- 初始版本发布
- 基础压缩功能
- 多线程支持
- 覆盖/输出模式
